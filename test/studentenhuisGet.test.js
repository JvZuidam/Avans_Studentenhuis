const assert = require("assert");
const chai = require("chai");
const chaiHttp = require("chai-http");
const mocha = require("mocha");

chai.should();
chai.use(chaiHttp);

describe("Studentenhuis Get", () => {
    it('', )
});

